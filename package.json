{
  "name": "live-trivia",
  "version": "1.0.0",
  "private": true,
  "description": "A trivia platform: questions are generated server-side from live internet sources when a backend is configured, and played from a bundled bank in the browser when one is not.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "migrate": "node scripts/migrate.js",
    "refresh": "node scripts/refresh.js",
    "build:bank": "node scripts/build-bank.js",
    "build:standalone": "node scripts/build-standalone.js",
    "build": "npm run build:bank && npm run build:standalone",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "world-countries": "^5.1.0"
  }
}
