{
  "name": "entropic-brainwaves",
  "version": "1.0.0",
  "private": true,
  "description": "An online-only trivia platform: trivia is generated server-side from live internet sources, cached, and served through a serverless API.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "migrate": "node scripts/migrate.js",
    "refresh": "node scripts/refresh.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "pg": "^8.13.1"
  }
}
